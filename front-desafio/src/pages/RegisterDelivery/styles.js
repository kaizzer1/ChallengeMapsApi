import { styled } from "@mui/material";

export const ContainerRegisterDelivery = styled("div")(() => ({
  width: "100%",
  height: "100vh",
}));

export const DivRegisterDelivery = styled("div")(() => ({
  width: "100%",
  height: "100%",
  display: "flex",
  justifyContent: "start",
  alignItems: "center",
}));

export const DivMapsDelivery = styled("div")(() => ({
  width: "100%",
  height: "100%",
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
}));
